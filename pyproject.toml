[project]
name = "test_github_actions"
description = "example"
version = "0.1.0"
authors = [
    { name = "Jakob Wall", email = "jakob.wall@outlook.com" }
]
dependencies = [
]

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]